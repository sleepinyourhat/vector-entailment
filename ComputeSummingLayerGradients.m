% Want to distribute this code? Have other questions? -> sbowman@stanford.edu
function [deltaLeft, deltaRight] = ...
      ComputeSummingLayerGradients(delta)
% Compute the gradients and deltas for a summing layer for a given example.

deltaRight = delta;
deltaLeft = delta;

end